# Node
node_modules/

# Build artifacts
dist/
build/
.cache/
.tmp/

# Elm
client/elm-stuff/

# Wrangler local dev state (D1/DO SQLite simulation, bundle cache)
.wrangler/

# Env
.env
.env.local
.env.*.local
.dev.vars
!.dev.vars.example

# Claude Code — local, machine-specific config (permission allowlists etc.).
# settings.local.json is already covered by the global gitignore; settings.json
# here only accumulates per-session approval cruft, so keep it out too. A
# deliberate shared config can be force-added later.
.claude/settings.json
.claude/settings.local.json
